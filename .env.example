# (必需) 所有浏览器实例将要访问的统一目标 URL
CAMOUFOX_INSTANCE_URL="https://aistudio.google.com/apps/drive/1CPW7FpWGsDZzkaYgYOyXQ_6FWgxieLmL"

# (可选) 浏览器运行模式。默认为 "virtual"。
# 可选值:
#   - "true": 完全无头模式，没有图形界面。
#   - "false": 正常模式，会显示浏览器窗口。
#   - "virtual": 虚拟显示模式，在无头环境下模拟一个显示器，兼容性最好。
# CAMOUFOX_HEADLESS="virtual"

# (可选) 设置代理服务器。如果不需要代理，请将此行注释掉或留空。
# 格式: http://<user>:<password>@<host>:<port> 或 http://<host>:<port>
# CAMOUFOX_PROXY="http://proxy.example.com:8080"

# (可选) 每个浏览器实例启动之间的延迟时间（秒），默认为 30 秒。
# 增加此值可以降低启动多个实例时的 CPU 峰值负载。
# INSTANCE_START_DELAY=30

# (可选) KeepAliveError 最大重试次数，默认为 5 次。
# 达到上限后，该实例将停止重启并退出，避免频繁重启消耗资源。
# MAX_RESTART_RETRIES=5

# (可选) 日志时区偏移量（小时）。默认为 8 (UTC+8 北京时间)。
# 设置为 0 即为 UTC 时间。
# TZ_OFFSET=8

# (可选) 项目根目录路径。通常不需要手动设置，程序会自动检测。
# 仅在特定的容器环境或自定义部署结构中需要。
# CAMOUFOX_PROJECT_ROOT="/app"

# (可选) 运行模式。
# 如果设置为 "true"，程序将以 Web 服务器模式运行（提供健康检查端点），适用于 HuggingFace Spaces。
# 如果未设置或为其他值，程序将以独立的 CLI 模式运行。
# HG="true"

# (必需/可选) 用户 Cookie 配置。
# 至少需要配置一个有效的 Cookie 来源（环境变量或 cookies/ 目录下的 JSON 文件）。
# 支持配置多个账号，通过后缀数字区分：_1, _2, _3 ...
# 值可以是 JSON 字符串（Cookie-Editor 导出格式）或 KV 字符串 ("name=value; name2=value2")。
# USER_COOKIE_1='[{"name": "__Secure-1PSID", "value": "..."}]'
# USER_COOKIE_2='__Secure-1PSID=...; __Secure-1PAPISID=...'